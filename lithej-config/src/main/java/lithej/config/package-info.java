/**
 * Configuration helpers: {@link lithej.config.Env} (environment variables),
 * {@link lithej.config.PropertiesX} ({@code .properties} file loading and typed
 * access), and {@link lithej.config.Config} (a small layered configuration source
 * combining several sources with explicit, documented precedence).
 */
package lithej.config;
